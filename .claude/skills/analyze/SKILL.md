# Codebase Analysis Skill

Analyze the entire project and produce a report with:
1. Issues categorized by severity (Critical/High/Medium/Low)
2. Specific file paths and line numbers
3. Concrete fix suggestions with code snippets
4. Summary table at the top

Output as a markdown file in ./reports/ with timestamp in filename.
